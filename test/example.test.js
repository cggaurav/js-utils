describe('debug', () => {

    const debug = require('../src/debug')

    test('import', () => {
        expect(debug).toBeInstanceOf(Function)
    })

    describe('debug()', () => {
        // TODO
    })

})
